我用 Flask 写了个系统监控的 API，代码在 api_flask.py 里。访问 /report 能看到 CPU、内存、磁盘和天气，数据存进 SQLite，用 crontab 每 5 分钟自动采一次。
最早其实就是个脚本，check_system.py，只能在终端里跑，关了就没了，别人也看不了。后来想让它能远程访问，就改成了 API，跑在 Linux 虚拟机上。
一开始只有 JSON，别人打开一看一堆大括号，不知道是什么。这周我就写了个页面，模板在 templates/index.html，访问 /dashboard 能看到上面三个卡片显示 CPU、内存、磁盘，下面用 Chart.js 画了条 CPU 折线图，看最近 10 次的变化。页面每 30 秒自动刷新一次。
CPU 和内存是直接读 Linux 的 /proc 文件，没装额外工具。天气和 AI 分析都是用 requests 调的接口。中间还加了 /analyze，把状态发给 Agnes AI 分析正不正常。
数据库文件是 monitor.db，里面就一张表 monitor_history，存时间、CPU、内存、磁盘。
踩的坑挺多的。有次浏览器打不开页面，我以为地址写错了，查了半天是防火墙没开 8001 端口。加上就好了。还有一次 API Key 写代码里推到 GitHub 了，被 GitGuardian 警告，后来改成从环境变量读。
做完这个项目，说实话感觉代码能跑只是第一步。能不能部署、别人能不能访问、数据存不存下来，这些才是真的。
GitHub：https://github.com/zzp-03/monitor
